# KLINOVA1 Telehealth App

This repository contains the Flutter telehealth application.

## Features
- Authentication
- Doctor Listing
- Appointment Management

## Installation
1. Clone the repository
2. Run `flutter pub get` to install dependencies
3. Run the app using `flutter run`
