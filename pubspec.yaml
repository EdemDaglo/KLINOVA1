name: Flutter Telehealth App


environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^5.0.0
  http: ^0.13.4
  flutter_secure_storage: ^5.0.2
  firebase_core: ^2.3.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^3.3.0
  video_player: ^2.2.10

flutter:
  uses-material-design: true
  assets:
    - assets/images/  
